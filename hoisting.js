/*It means we can call a function and define it below (después)*/

//Declarative
functionName();

function functionName(parameters) {
  return 0;
}

//Imperative
/*It doesn't work with the imperative function, It has to be declared before being called.*/
